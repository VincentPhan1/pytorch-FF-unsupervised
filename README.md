# Forward-Forward MNIST (Unsupervised Implementation)
This repository contains a Jupyter Notebook implementing [Geoffrey Hinton's Forward-Forward (FF) algorithm](https://www.cs.toronto.edu/~hinton/FFA13.pdf) on the MNIST dataset.
Unlike standard supervised MLPs that rely on backpropagation, this implementation explores the self-supervised/unsupervised approach.
